"use strict;"

        // construction fonction permettant de log un message dans la console avec un parametre message
        function sayMessage(msg)
        {
            console.log(msg);
        }
        //construction fonction pour annoncer le debut du programme
        function sayHello()
        {
            sayMessage("Debut du programme devine le nombre !");
        }
        //construction fonction pour annoncer la fin du programme
        function sayGoodbye()
        {
            sayMessage("Fin du programme devine le nombre !");
        }